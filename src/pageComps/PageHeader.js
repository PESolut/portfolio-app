import NavBar from '../components/NavBar'

function PageHeader() {
    return (
      <header className="page-header">
        <div className="logo">Page Header</div>
        <NavBar/>
      </header>
    );
  }
  
export default PageHeader